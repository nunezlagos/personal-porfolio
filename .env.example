# Copy to .env (local Node) or use .dev.vars for Wrangler local / Cloudflare Dashboard for production.
# Never commit .env or .dev.vars.

# Google OAuth (create at https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Session cookie (generate: openssl rand -hex 32)
SESSION_SECRET=your-32-byte-hex-secret

# Base URL for OAuth redirect (e.g. http://localhost:4321 in dev)
PUBLIC_APP_URL=http://localhost:3333
